<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
    <display-name>Integrador</display-name>
    <!-- ================================================================
         FILTROS — registrados só via @WebFilter nas próprias classes
         (AuthFilter.java, CharsetFilter.java), não aqui.
         ================================================================
         Antes havia um <filter>/<filter-mapping> aqui para cada um,
         duplicando o registro que a anotação @WebFilter já fazia
         sozinha — como os nomes não coincidiam entre a anotação e o
         web.xml, o Tomcat tratava como DOIS filtros distintos e cada
         um rodava duas vezes por requisição (visível nos logs: a
         mesma linha de doFilter aparecia duplicada na stack trace de
         qualquer erro em rota /app/*). Não quebrava nada — os dois
         filtros são idempotentes — mas era trabalho em dobro sem
         motivo. Removido; a anotação sozinha já é suficiente.
         O init-param de encoding do CharsetFilter que existia aqui
         (encoding=UTF-8) também foi removido sem impacto: é
         exatamente o valor padrão que a classe já usa quando nenhum
         init-param é fornecido (ver CharsetFilter.init()).
    -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config><http-only>true</http-only></cookie-config>
    </session-config>
    <error-page><error-code>403</error-code><location>/WEB-INF/views/error/403.jsp</location></error-page>
    <error-page><error-code>404</error-code><location>/WEB-INF/views/error/404.jsp</location></error-page>
    <error-page><error-code>500</error-code><location>/WEB-INF/views/error/500.jsp</location></error-page>
</web-app>
